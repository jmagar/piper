"use client";

import * as React from "react";
import { Card } from "@/components/ui/card";

/**
 * Client-side MCP Config Page component
 * This is a placeholder component that will be rebuilt
 */
export function ConfigClient() {
  return (
    <Card className="p-6">
      <p>The MCP Configuration component is currently being rebuilt. Please check back soon.</p>
    </Card>
  );
} 