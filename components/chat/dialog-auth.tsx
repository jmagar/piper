"use client"

// type DialogAuthProps = {
//   open: boolean
//   setOpen: (open: boolean) => void
// }

// Authentication dialog is not needed in admin-only mode
export function DialogAuth(/*{ open, setOpen }: DialogAuthProps*/) {
  return null
}
